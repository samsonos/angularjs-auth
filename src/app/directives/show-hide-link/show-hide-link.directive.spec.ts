import { ShowHideLinkDirective } from './show-hide-link.directive';

describe('ShowHideLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowHideLinkDirective();
    expect(directive).toBeTruthy();
  });
});
